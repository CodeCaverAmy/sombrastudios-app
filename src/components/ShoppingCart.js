import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class ShoppingCart extends Compondent {

  getInitialState = () => {
    return {
      
    }
  };

  propTypes: {

  };

  render() {
    return(

    );
  }
}
